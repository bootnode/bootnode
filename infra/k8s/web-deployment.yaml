apiVersion: apps/v1
kind: Deployment
metadata:
  name: bootnode-web
  namespace: bootnode
  labels:
    app: bootnode-web
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bootnode-web
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: bootnode-web
        component: frontend
    spec:
      containers:
        - name: web
          image: ghcr.io/hanzoai/bootnode:web-latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3001
              protocol: TCP
          env:
            - name: NODE_ENV
              value: "production"
            - name: NEXT_PUBLIC_BRAND
              value: "hanzo"
            - name: NEXT_PUBLIC_API_URL
              value: "https://api.web3.hanzo.ai"
            - name: NEXT_PUBLIC_WS_URL
              value: "wss://ws.web3.hanzo.ai"
            - name: NEXT_PUBLIC_IAM_URL
              value: "https://iam.hanzo.ai"
            - name: NEXT_PUBLIC_IAM_CLIENT_ID
              value: "hanzo-web3"
            - name: NEXT_PUBLIC_IAM_DOMAIN
              value: "hanzo.id"
            - name: NEXT_PUBLIC_AUTH_MODE
              value: "iam"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            allowPrivilegeEscalation: false
---
apiVersion: v1
kind: Service
metadata:
  name: bootnode-web
  namespace: bootnode
  labels:
    app: bootnode-web
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3001
      targetPort: http
      protocol: TCP
  selector:
    app: bootnode-web
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: bootnode-web-hpa
  namespace: bootnode
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: bootnode-web
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
